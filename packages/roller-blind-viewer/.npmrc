; Scoped publishes and installs of this package use GitHub Packages.
; Authenticate with a PAT that has read:packages (install) or write:packages (publish), or use GITHUB_TOKEN in CI.
@diloytd:registry=https://npm.pkg.github.com
